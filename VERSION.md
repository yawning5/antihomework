# Version History

빌드 산출물은 `output_v*` 폴더에 버전 단위로 보관합니다.

## output_v1

- 날짜: 2026-03-09
- 목적: 첫 번째 버전 빌드 산출물 정리
- 산출물:
  - `output_v1/`
- 비고:
  - 온보딩 문서는 루트 `ONBOARDING.md`에 별도 관리

## output_v2

- 날짜: 2026-03-09
- 목적: 키보드 매크로 기반 카카오톡 자동화 방식으로 전환
- 산출물:
  - `output_v2/`
- 비고:
  - 메인 창 탐색 + `Ctrl+F` 검색 + 붙여넣기 기반 전송 흐름 반영

## output_v3

- 날짜: 2026-03-09
- 목적: 입력 주입 방식을 `SendInput` 기반으로 보강
- 산출물:
  - `output_v3/`
- 비고:
  - `Ctrl+F`가 카카오톡에 전달되지 않던 문제 대응용 빌드

## output_v4

- 날짜: 2026-03-09
- 목적: `Ctrl+F` 전 카카오톡 클라이언트 영역 클릭으로 입력 포커스 보강
- 산출물:
  - `output_v4/`
- 비고:
  - 메인 창 활성화 후 내부 클릭을 수행한 뒤 단축키를 보내는 빌드

## output_v5

- 날짜: 2026-03-09
- 목적: `Ctrl+F` 단독 검증용 테스트 메뉴 추가
- 산출물:
  - `output_v5/`
- 비고:
  - 메인 창 활성화 후 검색 단축키만 보내는 진단 경로 포함

## output_v6

- 날짜: 2026-03-09
- 목적: `output_v2`와 동일한 동작 흐름으로 롤백
- 산출물:
  - `output_v6/`
- 비고:
  - `Ctrl+F` 테스트 메뉴, 내부 클릭 포커스 보강, `SendInput` 변경을 제거하고 v2 동작으로 복귀

## output_v7

- 날짜: 2026-03-09
- 목적: 채팅방 이름 붙여넣기 후 검색 반영 대기 시간 추가
- 산출물:
  - `output_v7/`
- 비고:
  - `Ctrl+F` 후 채팅방 이름 붙여넣고 Enter 전에 1초 대기

## output_v8

- 날짜: 2026-03-09
- 목적: WinForms 클라이언트 전환 및 PostgreSQL 연결 설정 기능 추가
- 산출물:
  - `output_v8/`
- 비고:
  - PostgreSQL 연결 정보 저장, 연결 테스트, SQL 조회, 카카오톡 수동 전송 UI 포함

## output_v9

- 날짜: 2026-03-09
- 목적: `chat_out` 자동 폴링 워커 및 순차 발송/즉시 삭제 흐름 추가
- 산출물:
  - `output_v9/`
- 비고:
  - 1초 단건 폴링, 시퀀스 완료 기반 성공 판정, 성공 시 즉시 삭제, 실패 시 워커 중지

## output_v10

- 날짜: 2026-03-09
- 목적: 폴링과 분리된 수동 진단 경로 추가
- 산출물:
  - `output_v10/`
- 비고:
  - `Ctrl+F` 단독 테스트와 수동 단건 메시지 전송 테스트 UI 포함

## output_v11

- 날짜: 2026-03-09
- 목적: 건별 발송 소요시간 JSONL 로그 추가
- 산출물:
  - `output_v11/`
- 비고:
  - `dispatch-log.jsonl`에 `polled_at`, `sequence_completed_at`, `duration_ms`, `duration_sec` 기록
